import { RemoveAllCbCheckedDirective } from './remove-all-cb-checked.directive';

describe('RemoveAllCbCheckedDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveAllCbCheckedDirective();
    expect(directive).toBeTruthy();
  });
});
