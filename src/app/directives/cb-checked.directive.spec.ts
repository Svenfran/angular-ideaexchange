import { CbCheckedDirective } from './cb-checked.directive';

describe('CbCheckedDirective', () => {
  it('should create an instance', () => {
    const directive = new CbCheckedDirective();
    expect(directive).toBeTruthy();
  });
});
